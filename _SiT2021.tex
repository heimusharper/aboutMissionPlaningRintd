% !TeX encoding = UTF-8
\input{_preambleSiT2021.tex}

% Начало сборника
\begin{document}
	% Приводим в порядок заголовок списка литературы
	\renewcommand{\refname}{\centerline{Список литературы}}
	% Установка размера шрифта для всего документа
	\fontsize{10pt}{12pt}\selectfont
	
%	\input{firstpage} % Первая страница сборника 
%	\input{secondpage}  % Вторая страница сборника 
	
    % Приводим к нормальному виду заголовок содержания
    \renewcommand{\contentsname}{\centerline{СОДЕРЖАНИЕ}}
	\tableofcontents % Вставляем содержание
	\clearpage % Переносим весь текст после содержания на новую страницу
	 		
	% Подключаем статьи авторов
	% Название поключаемого файла должно соответствовать наименованию файла автора в дирректории.
	% В названии фалов допускаются только буквы латинского алфавита и арабские цифры
	
	% Если начало новой главы попадает на четную страницу, необходимо перенести его на нечетную
	% для этого нужно добавить следующий код перед объявлением новой главы - 
	% \newpage\mbox{}\thispagestyle{empty}\clearpage
	% Объявление новой главы
	
	\input{Stepanov}

	%	\input{lastpage} % Последняя страница сборника
\end{document}
